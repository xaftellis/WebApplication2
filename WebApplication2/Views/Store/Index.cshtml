@model IEnumerable<Product>

@{
    ViewData["Title"] = "Home Page";
}

<h2>Product Grid Example</h2>

<div class="grid">
    @foreach (var item in Model)
    {
        var base64 = Convert.ToBase64String(item.Photo);
        var imgSrc = $"data:image/jpeg;base64,{base64}";

        <div class="product">
            <img src="@imgSrc" alt="@item.Name" />
            <div class="product-content">
                <div class="product-name">@item.Name</div>
                <div class="product-desc">@item.Description</div>
                <div class="product-price">$@item.Price</div>
                <div class="product-posted">Posted @item.DateCreated.ToString("dd") @item.DateCreated.ToString("MMM")</div>
            </div>
        </div>
    }

    @* <div class="product" onclick="alert('Clicked Product 1')">
        <img src="https://picsum.photos/seed/product1/300/150" alt="Product 1" />
        <div class="product-content">
            <div class="product-name">Product 1</div>
            <div class="product-desc">Short description of Product 1.</div>
            <div class="product-price">$29.99</div>
            <div class="product-posted">Posted Jan 25</div>
        </div>
    </div>

    <div class="product" onclick="alert('Clicked Product 2')">
        <img src="https://picsum.photos/seed/product2/300/150" alt="Product 2" />
        <div class="product-content">
            <div class="product-name">Product 2</div>
            <div class="product-desc">Short description of Product 2.</div>
            <div class="product-price">$39.99</div>
            <div class="product-posted">Posted Jan 26</div>
        </div>
    </div>

    <div class="product" onclick="alert('Clicked Product 3')">
        <img src="https://picsum.photos/seed/product3/300/150" alt="Product 3" />
        <div class="product-content">
            <div class="product-name">Product 3</div>
            <div class="product-desc">Short description of Product 3.</div>
            <div class="product-price">$49.99</div>
            <div class="product-posted">Posted Jan 27</div>
        </div>
    </div> *@
</div>

<style>
    body {
        font-family: Segoe UI;
        /* background: #f7f7f7; */
        /* padding 20px; */
    }

    .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
        gap: 20px;
    }

    .product {
        background: rgba(255,255,255,0.85); /* subtle blend with background */
        border-radius: 12px;
        box-shadow: 0 3px 10px rgba(0,0,0,0.05);
        cursor: pointer;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: all 0.2s ease;
    }

        .product:hover {
            background: #fff; /* solid white on hover */
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }

        .product:active {
            transform: translateY(0);
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        .product img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            /* faded look */
            opacity: 0.75;
            filter: saturate(0.9) brightness(0.95);
            transition: opacity 0.25s ease, filter 0.25s ease;
        }

        /* when hovering the card, restore image */
        .product:hover img {
            opacity: 1;
            filter: saturate(1) brightness(1);
        }



    .product-content {
        padding: 15px;
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .product-name {
        font-size: 18px;
        font-weight: bold;
        color: #333;
    }

    .product-desc {
        font-size: 14px;
        color: #666;
        flex-grow: 1;
    }

    .product-price {
        font-size: 16px;
        font-weight: bold;
        color: #1a73e8;
    }

    .product-posted {
        font-size: 12px;
        color: #999;
    }
</style>